export class Executor {
    private done: boolean = false;
    constructor(private context: any, private executor: Function, private resolve: Function, private reject: Function) {

    }

    run() {
        
    }
}